# Python
.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Synthetic dataset archives (re-derivable; see data/README.md)
data/*.tar.zst
data/extracted/
data/augmented/

# DIMACS raw files (downloadable on demand)
data/dimacs_raw/
dimacs_raw/
dimacs_processed/

# Run artifacts (regenerated by the notebook on demand) — only the top-level
# outputs/ is ignored, archive/outputs/ stays in the repo.
/outputs/
outputs_run.log

# Build / archive scratch
archives/
*.zip

# Editor / IDE
.vscode/
.idea/
